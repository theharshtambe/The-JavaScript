var shadow = 24;

{
   var shadow = 15;

   console.log(shadow);
}

console.log(shadow);

/*
In JS shadowing occurs when a variable in an inner scope has the same name as a variable in an outer scope

Inner varibale (shadow) is shadowing outer variable (shadow) 

let and const will work differently since both have variables will have different memory space i.e block, script
*/
