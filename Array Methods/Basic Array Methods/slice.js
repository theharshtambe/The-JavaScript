const items = ['item1', 'item2', 'item3', 'item4', 'item5'];
const discountedItems = items.slice(2, 5);

console.log(discountedItems);

// The slice() method returns a shallow copy of part of an array, without modifying the original array. You specify a starting and (optional) ending index.
