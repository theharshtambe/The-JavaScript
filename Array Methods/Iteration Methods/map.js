const prices = [100, 200, 300, 400, 500];

const discountedPrices = prices.map((price) => price * 0.9);

console.log(discountedPrices);

// The map() method creates a new array by transforming each element of the original array. It doesn’t modify the original array.
