const users = ['Riya', 'Shriya', 'Harsh', 'Arya', 'Ovee'];

users.forEach((user) => console.log(`Sending email to ${user}`));

// The forEach() method is used to iterate over an array and perform an action for each element. It doesn’t return anything.
