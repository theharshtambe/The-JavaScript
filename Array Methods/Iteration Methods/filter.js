const ages = [12, 18, 25, 15, 30];

const adultAges = ages.filter((age) => age >= 18);

console.log(adultAges);

// The filter() method creates a new array with only the elements that pass a specified condition.
