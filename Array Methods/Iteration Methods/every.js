const scores = [85, 90, 78];

const allHighQuality = scores.every((score) => score > 75);

console.log(allHighQuality);

// The every() method checks if all elements in the array satisfy a condition. It returns true or false.
