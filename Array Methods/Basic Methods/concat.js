const riyaFavouriteColors = ['black', 'blue'];
const harshFavouriteColors = ['red', 'white'];

const combinedColors = riyaFavouriteColors.concat(harshFavouriteColors);

console.log(combinedColors);

// The concat() method merges two or more arrays into one. It doesn’t change the original arrays.
